# This is the PUBLIC portfolio repo. private/ has its own separate repo and
# its own .gitignore (see private\.gitignore) — it must never be added here.
private/

# Build output for the Cloudflare deploy (regenerated by deploy.bat)
site/

# Tooling
node_modules/
.wrangler/
